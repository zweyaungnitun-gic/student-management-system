package com.gicm.student_management_system.service;

/* 
public interface StudentService {

    // ---- Student Main ----
    List<Student> getAllStudents();
    Student getStudentById(Long id);
    Student createStudent(Student student);
    Student updateStudent(Long id, Student student);
    void deleteStudent(Long id);

    // ---- Class Enrollment ----
    ClassEnrollment getClassEnrollment(Long studentId);
    ClassEnrollment saveClassEnrollment(Long studentId, ClassEnrollment enrollment);

    // ---- Exam Results ----
    ExamResult getExamResult(Long studentId);
    ExamResult saveExamResult(Long studentId, ExamResult examResult);

    // ---- Speaking Class ----
    SpeakingClass getSpeakingClass(Long studentId);
    SpeakingClass saveSpeakingClass(Long studentId, SpeakingClass data);

    // ---- Speaking Class Result ----
    SpeakingClassResult getSpeakingClassResult(Long studentId);
    SpeakingClassResult saveSpeakingClassResult(Long studentId, SpeakingClassResult result);

    // ---- Teacher Assignment ----
    TeacherAssignment getTeacherAssignment(Long studentId);
    TeacherAssignment saveTeacherAssignment(Long studentId, TeacherAssignment teacher);
}
*/
